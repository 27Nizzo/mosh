#tuples são como listas, utilizados para guardar sequencias de objetos, mas no caso dos tuples eles imutáveis
#ou seja, são é possivel modifica-los depois de os criar

numbers = (1,2,3,3)
print(numbers.count(3))
print(numbers.index(1))

# em tuples podemos usar as funções pre-definidas, como:
# -> .count(elemento): que vai contar quantas vezes este elemento ocorre na lista

